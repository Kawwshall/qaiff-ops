[project]
name = "qaiff_ops"
version = "0.0.1"
description = "QAIFF Ops - Factory Operations ERP"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "qaiff_ops"
